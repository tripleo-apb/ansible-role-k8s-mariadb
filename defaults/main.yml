coe_host:
coe_config_context:
coe_config_file:
action: provision

service_name: mariadb
service_account_name: 
namespace: openstack

mariadb_config:
  host: '{{service_name}}'
  root_password: weakpassword
